# 仿真转真机验证清单

当前总状态：`PENDING_HARDWARE`

本机未连接海康相机和真实机械臂。下面所有条目必须在实验室逐项执行、记录和签字；仿真自动通过不计作硬件通过。

## 1. 软件与连接

- [ ] `PENDING_HARDWARE` 确认真实机械臂型号、固件和通信地址。
- [ ] `PENDING_HARDWARE` 确认控制电脑与机械臂处于隔离、可控网络。
- [ ] `PENDING_HARDWARE` Windows 海康 MVS 安装路径和版本已记录。
- [ ] `PENDING_HARDWARE` Linux MVS 运行库路径、`LD_LIBRARY_PATH` 和权限已记录。
- [ ] `PENDING_HARDWARE` 海康相机能够枚举，序列号与实验台编号一致。
- [ ] `PENDING_HARDWARE` 相机连续取帧，无超时、花屏和丢帧异常。

## 2. 海康相机参数

- [ ] `PENDING_HARDWARE` 曝光时间已固定并记录。
- [ ] `PENDING_HARDWARE` 增益、白平衡和像素格式已记录。
- [ ] `PENDING_HARDWARE` 连续触发或软触发模式与程序设置一致。
- [ ] `PENDING_HARDWARE` 分辨率与标定、识别配置一致。
- [ ] `PENDING_HARDWARE` 工作区四角和所有标定点均清晰可见。

## 3. 真实标定

- [ ] `PENDING_HARDWARE` 清除仿真标定矩阵，不直接复用。
- [ ] `PENDING_HARDWARE` 在真实工作平面重新采集至少三个不共线拟合点。
- [ ] `PENDING_HARDWARE` 使用至少一个独立验证点。
- [ ] `PENDING_HARDWARE` 记录 RMS、最大误差和坐标轴方向。
- [ ] `PENDING_HARDWARE` 复核相机安装位置、工作台高度和镜头畸变影响。

## 4. TCP、Z 高度与吸盘

- [ ] `PENDING_HARDWARE` 真实吸盘 TCP 已测量并写入真机配置。
- [ ] `PENDING_HARDWARE` 抓取 Z 从高位逐步下降确定，不照搬仿真值。
- [ ] `PENDING_HARDWARE` 安全高度、释放高度和 TCP 偏差已记录。
- [ ] `PENDING_HARDWARE` 真空建立、保持、释放和漏气检查正常。
- [ ] `PENDING_HARDWARE` 软管不会进入关节、相机或工作区碰撞路径。

## 5. 安全

- [ ] `PENDING_HARDWARE` 急停按钮有效，教师和学生都能立即触达。
- [ ] `PENDING_HARDWARE` 速度、加速度和工作空间限制已设置。
- [ ] `PENDING_HARDWARE` 工作区已清场，人员不进入机械臂运动范围。
- [ ] `PENDING_HARDWARE` 先执行不吸附、不接触物体的低速空跑。
- [ ] `PENDING_HARDWARE` 低速空跑全过程无碰撞、奇异位和超限。

## 6. 分级验证

- [ ] `PENDING_HARDWARE` 单点移动：三个已知世界坐标逐点低速到达。
- [ ] `PENDING_HARDWARE` 单物体抓取：只放置一个物体，完成识别、吸附、抬升和释放。
- [ ] `PENDING_HARDWARE` 单物体抓取重复至少 5 次并记录成功率。
- [ ] `PENDING_HARDWARE` 多物体分类：逐步增加到 6 个对象。
- [ ] `PENDING_HARDWARE` 多物体分类中每次重新取帧，无重复抓取。
- [ ] `PENDING_HARDWARE` 所有物体稳定落入正确分类区。

## 7. 证据和签字

- [ ] `PENDING_HARDWARE` 保存海康原始帧、标定叠加图和识别叠加图。
- [ ] `PENDING_HARDWARE` 保存低速空跑、单物体抓取、多物体分类视频。
- [ ] `PENDING_HARDWARE` 保存配置快照、事件日志和错误记录。
- [ ] `PENDING_HARDWARE` 学生签字、教师签字、实验台编号和日期齐全。

禁止性结论：

- 没有当次实验室证据和签字，不得宣称海康相机完成实机验收。
- 安全、低速空跑、单物体抓取和多物体分类未全部完成，不得宣称真实机械臂完成验收。
